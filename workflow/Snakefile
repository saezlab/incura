from snakemake.utils import min_version

min_version('7.14.0')

configfile: 'config/config.yaml'
#container: 'workflow/envs/incura.sif'

ORGANISM = config['organism']

rule all:
    input: 
        "data/promoters.fa"

#------------------------------------------------------------------------------------

module getPromoters:
    snakefile: "rules/getPromoters.smk"
    config: config

use rule processPromoters from getPromoters


module getPromoters:
    snakefile: "rules/getPromoters.smk"
    config: config

use rule createFasta from getPromoters


# snakemake --profile config/slurm/ ...
